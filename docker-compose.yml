services:
  mineflayer-mcp:
    build: .
    image: mc-mcp
    stdin_open: true
    network_mode: host
    environment:
      - MC_HOST=localhost
      - MC_PORT=25565
      - MC_USERNAME=mcp-bot
      - MC_AUTH=offline
      - VIEWER_PORT=3000
