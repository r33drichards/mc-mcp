{
  "mcpServers": {
    "mineflayer-mcp": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-p", "3000:3000",
        "-e", "MC_HOST=localhost",
        "-e", "MC_PORT=25565",
        "-e", "MC_USERNAME=mcp-bot",
        "-e", "MC_AUTH=offline",
        "mc-mcp"
      ]
    }
  }
}
